import React from 'react';
import Header from './Header/Header';

const App = () => {
    return (
        <React.Fragment>
            <Header/>
        </React.Fragment>
    );
};

export default App;
